{
  "$schema": "../node_modules/wrangler/config-schema.json",
  "name": "hananitsuku-answer",
  "main": "src/index.js",
  "compatibility_date": "2026-09-01",
  "workers_dev": true,
  "observability": {
    "enabled": true,
    "head_sampling_rate": 1
  },
  "vars": {
    // Candidate models, tried in order. First one that answers wins.
    // 2026-09-07 production check: reasoning models (gemma-4, gpt-oss-20b) can burn the whole completion budget on
    // reasoning and return empty content; qwen3 answered reliably and cheapest, so it goes first.
    "AI_MODELS": "@cf/qwen/qwen3-30b-a3b-fp8,@cf/openai/gpt-oss-20b,@cf/google/gemma-4-26b-a4b-it",
    // Global cap per UTC day (keeps usage inside the 10,000 Neurons/day free allocation; qwen3 ~13-25 neurons/request).
    "DAILY_LIMIT": "400",
    // Per visitor (hashed IP) caps.
    "PER_IP_MINUTE": "6",
    "PER_IP_DAY": "40",
    // Browser origins allowed to call this API (localhost is always allowed).
    "ALLOWED_ORIGINS": "https://yoshihikomizuno.github.io,https://mazareal.co.jp,https://www.mazareal.co.jp"
  },
  "ai": {
    "binding": "AI"
  },
  "durable_objects": {
    "bindings": [
      {
        "name": "QUOTA",
        "class_name": "QuotaCounter"
      }
    ]
  },
  "exports": {
    "QuotaCounter": {
      "type": "durable-object",
      "storage": "sqlite"
    }
  }
}
